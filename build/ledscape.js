// Generated by CoffeeScript 1.9.0
(function() {
  var bindir, glob, path, pixels, strips, wrapper;

  path = require("path");

  wrapper = require("./build/Release/wrapper");

  glob = require("glob");

  module.exports = wrapper;

  pixels = 64;

  strips = 48;

  bindir = path.resolve(__dirname, "../build");

  process.chdir(bindir);

  wrapper.init(pixels, strips);

  glob("anim/*.js", {}, function(er, files) {
    var file, name, tokens, _i, _len, _results;
    _results = [];
    for (_i = 0, _len = files.length; _i < _len; _i++) {
      file = files[_i];
      tokens = file.split('/');
      name = tokens[0];
      _results.push(wrapper[name] = require(bindir + '/' + file));
    }
    return _results;
  });

}).call(this);
